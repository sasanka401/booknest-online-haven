
# Supabase Integration

- `useBooks.ts`: Simple React hook to fetch all books using React Query and Supabase.
- `useCreateOrder.ts`: Async function to create an order and corresponding order_items rows in the Supabase database.

Import and use these functions in your pages as needed.
